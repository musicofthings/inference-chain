kind: interaction_update
schema_version: "1.0.0"
id: upd_06_sqlite_blocker
project_id: task-api
iteration: 1
created_at: "2026-05-22T15:00:00Z"
trigger: new_blocker
what_changed: "SQLite concurrent writes fail without WAL mode — production blocker discovered"
new_blockers:
  - "SQLite default journal mode locks on concurrent writes"
new_risks:
  - "production concurrency was not in original test plan"
do_not_repeat_delta:
  - "use in-memory SQLite for concurrent write workloads"
next_action_delta:
  - "enable WAL mode in SQLite bootstrap"
human_note: "Critical: ship-blocker for the API"
